---
name: "finance-bp-workflow"
description: "财务BP行业/公司业务逻辑分析工作流:需求解析→规范装载→七视角分析→报告落盘→名词沉淀→记忆归档→规范复盘,内置5份分析规范、报告骨架、指标模板与种子词库,支持跨会话记忆。当用户要求拆分/分析某行业或公司的业务逻辑、对比同行业公司模式、沉淀财务知识库时触发。"
---

# 财务 BP 业务逻辑分析工作流(Finance BP Workflow)

统筹「从分析需求 → 报告落盘 → 记忆归档 → 规范复盘」的完整流程,面向财务 BP(Business Partner)的行业与公司业务逻辑分析场景。

核心价值:

1. **每次分析都走同一条清晰路径**——七视角方法论 + 8 模块报告结构,不丢步骤、不返工
2. **跨会话记忆不丢失**——用户画像、约定、会话摘要沉淀在 skill 内置 `memory/` 记忆库
3. **方法论可随使用持续进化**——规范复盘形成修改建议,用户确认后固化到自己的规范副本

---

## 一、目录与路径约定

本 skill 自包含全部方法论资源,分两处落点:

### skill 目录内(随技能安装,出厂基准)

| 路径 | 职责 |
| ---- | ---- |
| `reference/` | 5 份规范(角色/输入/输出/框架/行为)+ 项目规范地图(快速索引) |
| `examples/` | 报告骨架示例 + 电商行业完整样例报告 |
| `templates/` | 关键指标清单模板(CSV,流量/交易/盈利三层结构) |
| `starter/` | 名词解释种子词库(首次初始化时复制到工作区) |
| `memory/` | 记忆库:`project-memory.md`(长期记忆)+ `session-summaries/`(会话摘要),首次运行后生成 |

### 用户工作区内(产出落点,首次运行自动创建)

| 目录 | 职责 |
| ---- | ---- |
| `00_Agent规范/` | 自定义规范副本(可选;从 `reference/` 复制后自由修改,**优先级高于出厂版**) |
| `01_分析产出/行业/`、`01_分析产出/公司/` | 分析报告落盘位置 |
| `02_知识库/` | 名词解释、年报速读、行业全景等知识沉淀 |
| `03_模板库/` | 可复用模板 |

**规范读取优先级**:工作区 `00_Agent规范/`(存在时)> skill `reference/`(出厂默认)。

---

## 二、首次使用初始化(第 0 步,仅执行一次)

接到首次分析任务、且工作区缺少产出目录时,先完成初始化:

1. 创建目录:`01_分析产出/行业/`、`01_分析产出/公司/`、`02_知识库/`、`03_模板库/`
2. 复制 `templates/关键指标清单模板.csv` → 工作区 `03_模板库/`(已存在则不覆盖)
3. 复制 `starter/名词解释.md` → 工作区 `02_知识库/名词解释.md`(已存在则不覆盖)
4. 复制 `memory/project-memory.template.md` → `memory/project-memory.md`(已存在则不复制,避免覆盖已有记忆)
5. 告知用户初始化完成,随后正常进入 9 步流程

**自定义规范(可选)**:用户希望修改方法论时,把 `reference/` 下 5 份规范复制到工作区 `00_Agent规范/` 再改;此后以工作区版本为准,skill 出厂文件保持不动。

---

## 三、落盘权限边界

| 对象 | 改动是否需先询问 | 说明 |
| ---- | ---------------- | ---- |
| 工作区 `00_Agent规范/` | **是(必须)** | 规范是全局规则源,第 9 步仅在用户同意后修改 |
| `01_分析产出/` | 否 | 分析报告直接落盘 |
| `02_知识库/` | 否 | 名词/速读/全景资料直接落盘 |
| `03_模板库/` | 否 | 模板直接落盘 |
| skill `memory/` | 否 | 项目记忆与会话摘要直接落盘(见第六节) |
| skill `reference/`、`examples/` 等出厂文件 | 否(且不应改) | 出厂基准,日常使用中不修改;用户定制走工作区 `00_Agent规范/` |

> 核心原则:**只有改"规则(规范)"要用户点头,改"内容(产出/知识/模板/记忆)"直接动手。**

---

## 四、完整工作流程(9 步)

每次分析任务,按此顺序编排:

| 步骤 | 做什么 | 依据 | 产出 | 落点 |
| ---- | ------ | ---- | ---- | ---- |
| 1 需求解析 | 把用户指令解析成 `subject_type / subject / scope / depth / focus / level / style` | `reference/02_输入规范.md` | 分析参数 | 对话(不落盘) |
| 2 规范与记忆装载 | 先读 `reference/项目规范地图.md` 定位条款,按需读 5 份规范原文(工作区有自定义副本则读副本);读 `memory/project-memory.md`;断点续接时加读最近 1~2 份会话摘要 | 规范地图 + 本文件第六节 | 生效规范 + 用户画像/约定快照 | 对话 |
| 3 知识准备 | 检索工作区 `02_知识库/`,必要时联网补最新数据并留来源 | `reference/05_行为规范.md`(数据真实) | 数据 + 来源 | 对话 |
| 4 分析执行 | 按七视角 + 三条主线展开(聚焦用户 `focus`,其他精简);报告骨架参考 `examples/报告骨架示例.md` | `reference/04_分析框架.md` | 分析内容 | 对话 |
| 5 落盘 | 生成 markdown,写入对应子目录;完整样例见 `examples/电商行业业务逻辑拆分_样例.md` | `reference/03_输出规范.md` | 分析报告 | `01_分析产出/{行业\|公司}/` |
| 6 名词沉淀 | 检索名词解释库 → 报告新术语且缺失则追加 | 本文件第五节 | 新术语条目 | `02_知识库/名词解释.md` |
| 7 记忆沉淀 | **每次对话收尾**写会话摘要;出现新习惯/约定/决策 → 更新项目记忆 | 本文件第六节 | 会话摘要 / 项目记忆更新 | skill `memory/` |
| 8 规范复盘 | 对照 5 份规范,识别本次暴露的"摩擦点" | 本文件第七节 | 更新建议 | 对话 |
| 9 规范更新 | 用户同意 → 修改工作区 `00_Agent规范/`(无副本则先从 `reference/` 复制) | 本文件第七节 | 规范新版本 | 工作区 `00_Agent规范/*.md` |

> 第 1~7 步是"做事 + 沉淀",第 8~9 步是"把经验固化回规范"。**前 7 步产出分析与记忆价值,后 2 步产出规范复用价值。**

---

## 五、名词沉淀规则(第 6 步)

每次报告落盘后(第 5 步完成),执行名词沉淀,让 `02_知识库/名词解释.md` 持续积累:

1. **检索现有词库**:先读取 `02_知识库/名词解释.md`,确认已有词条与同义词。
2. **扫描报告术语**:从本次报告中找出值得注意的专业名词——财务/业务指标、行业黑话、商业模式术语等。
3. **对比去重**:已有词条或其同义词、等价表述的,不重复创建。
4. **判断「值得注意」**:满足至少一条才收录——
   - 是财务/业务核心指标(如净利率、ROE、DSO、GMV、Take Rate);
   - 是行业特有黑话或新概念(如 GEO、CCC、双边网络效应);
   - 未来分析会复用,且非浅显常识。
5. **按统一格式直接追加**:沿用词库现有格式(`## 术语｜英文全称 中文名` + 定义/公式/通俗理解/注意事项,一词一条,`---` 分隔),追加到文件末尾。
6. **直接落盘**:名词解释属知识库积累,识别到缺失且值得收录即直接追加,无需额外审批。

---

## 六、记忆沉淀规则(第 7 步)

记忆库位于**本 skill 目录内** `memory/`,随 skill 走。文件名用英文,分两层:

| 层级 | 文件 | 性质 | 内容 |
| ---- | ---- | ---- | ---- |
| 长期记忆(语义) | `memory/project-memory.md` | 持续增量更新,开工必读 | 用户画像与偏好、项目约定与决策、可复用经验教训 |
| 情景记忆(会话) | `memory/session-summaries/YYYY-MM-DD_session-summary_{topic}.md` | 一次对话一份,只追加不改写 | 本次对话的诉求、产出、决策、待办、文件清单 |

> **只读降级**:若所在平台安装后 skill 目录不可写,则把记忆库改写到**工作区** `.finance-bp-memory/`(同样含 `project-memory.md` 与 `session-summaries/`),读写规则不变,并在摘要中注明实际落点。

**6.1 写会话摘要的时机(每次对话必存,不限于长对话)**

- 每次对话收尾即写:任务完成、用户结束话题、或上下文即将被压缩时
- 短对话写紧凑版(六段保留,每段 1~2 行);长对话写完整版
- 同一天多段小对话合并到当日一份摘要文件追加章节,不每段单开文件

**6.2 摘要六段格式**

① 用户诉求 ② 已完成产出 ③ 关键决策与约定 ④ 待办与下一步 ⑤ 涉及文件 ⑥ 可入长期记忆的线索

**6.3 项目记忆更新规则**

- **收录**:长期有效的画像/目标、沟通偏好、项目约定、用户拍板的决策、可复用经验教训
- **不收录**:一次性任务进度、原始素材、敏感信息、与项目无关的私人数据
- **方式**:增量更新对应分区,每条带来源日期;用户说"以后都这样/记住这个偏好"时**立即**更新

**6.4 装载规则(与第 2 步联动)**

- 每次任务开始:先读 `memory/project-memory.md`,让画像与约定自动生效,不重复追问
- 断点续接:先读 `memory/session-summaries/` 最新相关的 1~2 份,再动手
- 记忆与规范冲突:以生效规范为准,第 8 步复盘时提出对齐建议

---

## 七、规范复盘与更新(收尾环节)

> 规范更新是**唯一需要用户确认后才落盘**的环节;产出/知识/模板/记忆均直接落盘(见第三节)。

### 更新对象与方式

- **用户定制改工作区副本**:规范修改落在工作区 `00_Agent规范/`;若该目录不存在,先把 `reference/` 对应文件复制过去再改,skill 出厂文件保持不动
- **增量修改**:精确替换目标段落,不整文件重写
- **带变更说明**:在文件头部追加「变更说明」区块(版本号 + 本次变更 + 动机)
- **同步索引**:改完规范后,同步检查工作区 `00_Agent规范/项目规范地图.md`(若已复制)的条款索引是否需要更新

### 复盘判断标准

只有同时满足两条,才提出修改建议:

- **会复现**:下次同类任务还会踩
- **可落地**:能写成一条明确可执行的规则

如果本次分析顺畅、无摩擦,则直接说「本次无需更新规范」,**不硬凑**。

### 建议输出模板

```markdown
### 建议 N:一句话标题

- **目标文件**:01~05 之一
- **位置**:具体到节(如 04 分析框架 → 4.6 利润池)
- **类型**:新增 / 修改 / 删除 / 补充示例 / 修正歧义
- **改为**:可直接落盘的新表述
- **理由**:本次分析中哪一步、什么场景暴露了这个问题
```

---

## 质量约束

1. 每个建议都能追溯到本次真实场景?
2. 是否为了"完整"而加不必要规则?
3. 是否检查了跨文件引用的一致性?
4. 是否保留了可回滚性(版本号 + 变更说明)?
5. 是否有更简单的替代方案?
6. 会话摘要能否让"没参加过本次对话的人"无缝接手?项目记忆是否只收长期有效信息、没收临时进度?

---

## 一句话总结

> **按 9 步走完每次分析:开工先装载规范与记忆,收尾必存会话摘要;规范只等用户同意后改工作区副本,出厂文件不动、不自动乱动。**
