# 项目记忆(长期记忆模板)

> 用途:跨会话记住"用户是谁、习惯什么、项目有哪些约定"。由 finance-bp-workflow 技能第 2 步(开工装载)、第 7 步(收尾沉淀)维护。
> 规则:只收**长期有效**信息;增量更新、每条带来源日期;一次性进度写到「会话摘要」,不写这里。
> 本文件是空白模板:首次运行时由 `project-memory.template.md` 复制生成 `project-memory.md`,之后在副本上增量更新。

---

## 一、用户画像

<!-- 示例(首次对话后由助手填写):
- **身份**:财务/经营分析方向实践者,关注 XX 行业(YYYY-MM)
- **分析偏好**:业务逻辑优先,财务指标是业务结果的翻译(YYYY-MM)
-->

## 二、沟通与表达偏好

<!-- 示例:
- **语言**:中文沟通、中文落盘(YYYY-MM)
- **排版**:大表格偏好卡片式呈现(YYYY-MM)
-->

## 三、项目约定与规则

<!-- 示例:
- **落盘权限**:除 `00_Agent规范/` 改动需用户同意外,产出/知识/模板/记忆直接落盘(YYYY-MM)
- **文件命名**:日期前缀 `YYYY-MM-DD_主题.md`(YYYY-MM)
-->

## 四、已拍板的决策记录

<!-- 示例:
- **YYYY-MM-DD**:指标分类采用「流量层/交易层/盈利层」三层结构
-->

## 五、经验与教训(可复用)

<!-- 示例:
- **卡片式排版**:大表格用图标 + 分隔线 + 卡片框,可读性显著提升(YYYY-MM)
-->

---

> 会话级经过见 `memory/session-summaries/`(与本文件同目录);维护规则见 SKILL.md「记忆沉淀规则」。
